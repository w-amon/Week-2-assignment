/* Write a JavaScript program that demonstrates explicit type conversion to a number.
Perform the following steps:
1. Declare a variable `numberStr` and assign it a string value, e.g., `"42"`.
2. Use explicit type conversion to convert `numberStr` to a number using the 
`Number()` function.
3. Display the converted number in the console.
Explain in comments how you explicitly converted the string to a number using the 
`Number()` function.*/


var numberStr = "20"; //Declare a variable (numberStr) and assign it a string value

var convertedNumber = Number(numberStr); //Convert 'numberStr' to a number using the 'Number()' function

console.log("Converted Number:", convertedNumber); //Display the converted number in the console
